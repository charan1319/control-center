<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <!-- Full-bleed background (required for maskable icons — fills any clip shape) -->
  <rect width="512" height="512" fill="#1e1e2e"/>
  <!-- >_ terminal prompt — centered within maskable safe zone (inner 80% = 205px..307px) -->
  <text
    x="256"
    y="316"
    font-family="monospace"
    font-size="168"
    font-weight="bold"
    text-anchor="middle"
    fill="#cba6f7"
  >&gt;_</text>
</svg>
